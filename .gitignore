build/
data/
results.json


# ============================ LaTeX-related ignores ===========================

# At least for now, ignore bibliography files because those are generated by
# Zotero, not manually, so no reason to have to track (and constantly update)
# them here
*.bib

*.aux
*.bbl
*.bbl-SAVE-ERROR
*.pdf
